{
  "scripts": {
    "db:init": "sequelize init",
    "db:create": "sequelize db:create --env",
    "db:migrate": "sequelize db:migrate --env",
    "db:seed": "sequelize db:seed:all --env",
    "db:model:gen": "sequelize model:generate",
    "db:seed:gen": "sequelize seed:generate",
    "test": "NODE_ENV=test mocha --exit",
    "cv": "NODE_ENV=test  nyc --reporter=lcov --reporter=text mocha --exit",
    "fmt": "prettier --write **/*.js"
  },
  "dependencies": {
    "csvtojson": "^2.0.8",
    "mysql2": "^1.6.1",
    "sequelize": "^4.38.0"
  },
  "devDependencies": {
    "mocha": "^5.2.0",
    "nyc": "^12.0.2",
    "prettier": "^1.14.2",
    "sequelize-cli": "^4.0.0"
  },
  "prettier": {
    "singleQuote": true
  }
}
